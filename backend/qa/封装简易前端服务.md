# 封装简易前端服务

我想要将下面的 sh 指令执行过程封装成一个 systemd service:

```bash
cd ~/YATA/backend
source ./.venv/bin/activate
streamlit run src/streamlit_app.py
```

这个服务应该命名为 `yata-tiny-frontend`, 并将其添加到开机自启中.

请问我应该怎么做?
